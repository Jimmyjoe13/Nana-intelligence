// Dummy file to prevent ESLint from failing when targeting the 'pages' directory.
// This is necessary because ESLint returns a non-zero exit code if a targeted directory is empty or has no matching files.
export default function Dummy() {
  return null;
}
